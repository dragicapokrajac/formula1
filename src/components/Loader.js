import { MoonLoader } from "react-spinners";

const Loader = () => {
   return (
      <div className="loader-container">
         <MoonLoader color="#f91536" size={100} />
      </div>
   );
};

export default Loader;